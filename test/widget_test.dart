// Basic widget test for MatchLog app

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // TODO: Add widget tests when the app is fully configured
    expect(true, isTrue);
  });
}
